var combyne = require('../');

// Nested else statement with false nested value
  var tmpl = combyne('{%if test == "hello"%}hello{%elsif test == "goodbye"%}world{%else%}lol{{%endif%}', { test: "goodbye" });
  console.log( tmpl.render(), 'world', 'Truthy else-if' );
